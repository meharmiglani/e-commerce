class CartItem{
    constructor(quantity, price, productTitle, sum){
        this.quantity = quantity;
        this.price = price;
        this.productTitle = productTitle;
        this.sum = sum;
    }
}

export default CartItem;